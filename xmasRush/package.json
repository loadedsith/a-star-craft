{
  "name": "xmasrush",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jasmine",
    "watchtest": "nodemon --ignore bundle.js --ext js,jade --exec 'npm run test'",
    "watch": "nodemon --ignore bundle.js --ext js,jade --exec 'npm run build'",
    "build": "npx browserify index.js -o bundle.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^16.2.3",
    "jasmine": "^3.3.1"
  }
}
